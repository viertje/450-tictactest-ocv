# gh-pages

This branch is generated. Do not edit `coverage-history.csv` by hand — the
"Build & Test" workflow (`.github/workflows/build.yml`) appends one row to it
after every push to `main`, then pushes the update here itself.

`index.html` renders it as a chart via GitHub Pages.
